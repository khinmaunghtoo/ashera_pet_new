import 'package:flutter/cupertino.dart';

class SystemBack extends StatelessWidget {
  final Widget child;
  const SystemBack({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
